# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
dist-android/
dist-web/
web-build/
expo-env.d.ts

# app.json: a config é DINÂMICA (app.config.ts). O `expo install` às vezes recria um
# app.json estático ao lado, e aí passam a existir duas fontes de verdade brigando —
# o estático vence em algumas rotas do CLI. Se ele reaparecer, é pra apagar.
app.json

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

example

# generated native folders
/ios
/android
