# Dependências
node_modules/
package-lock.json

# iOS (gerado por `cap add ios` — Pods e build não vão pro git)
ios/App/Pods/
ios/App/App/public/
ios/App/build/
ios/DerivedData/
*.xcuserstate

# Android (gerado por `cap add android`)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/app/src/main/assets/public/
android/capacitor-cordova-android-plugins/

# Firebase config — credenciais, NUNCA commitar
google-services.json
GoogleService-Info.plist
